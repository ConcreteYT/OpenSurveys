/**
 * Google OAuth Web Client ID from `REACT_APP_GOOGLE_CLIENT_ID` in `.env`
 * (see `.env.example`). Must match `GOOGLE_CLIENT_ID` in the backend `.env`.
 */
export const GOOGLE_CLIENT_ID = process.env.REACT_APP_GOOGLE_CLIENT_ID || ''
